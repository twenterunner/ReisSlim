const ENDPOINTS=['https://overpass.private.coffee/api/interpreter','https://overpass-api.de/api/interpreter'];const valid=p=>p&&Number.isFinite(Number(p.lat))&&Number.isFinite(Number(p.lon)),rad=x=>x*Math.PI/180;function km(a,b){if(!valid(a)||!valid(b))return Infinity;const R=6371,d1=rad(b.lat-a.lat),d2=rad(b.lon-a.lon),x=rad(a.lat),y=rad(b.lat),h=Math.sin(d1/2)**2+Math.cos(x)*Math.cos(y)*Math.sin(d2/2)**2;return 2*R*Math.asin(Math.min(1,Math.sqrt(h)))}function typeAllowed(tags,requested,vehicle){const tourism=tags?.tourism,camping=['camp_site','caravan_site'].includes(tourism),lodging=['hotel','guest_house','hostel','motel'].includes(tourism);if(requested==='camping')return camping;if(requested==='hotel-bnb')return lodging;if(['motorhome','caravan'].includes(vehicle))return camping;return camping||lodging}export function motorcycleAccommodationScore(tags={}){let score=0;const motorcycle=String(tags.motorcycle||tags['access:motorcycle']||'').toLowerCase();if(['yes','designated','permissive'].includes(motorcycle))score+=38;if(['no','private'].includes(motorcycle))score-=80;const parking=String(tags.parking||tags['parking:condition']||'').toLowerCase();if(parking&&parking!=='no')score+=18;if(String(tags.garage||tags['parking:garage']||'').toLowerCase()==='yes')score+=28;if(String(tags.covered||tags['parking:covered']||'').toLowerCase()==='yes')score+=18;if(tags.website||tags['contact:website'])score+=6;if(tags.phone||tags['contact:phone'])score+=4;if(['camp_site','caravan_site'].includes(tags.tourism))score+=8;return score}export function accommodationSuitability(tags,trip,distanceKm=0){const vehicle=trip?.transport,requested=trip?.accommodationType||'any';if(!typeAllowed(tags,requested,vehicle))return-Infinity;let score=120-distanceKm*5;if(vehicle==='motorcycle')score+=motorcycleAccommodationScore(tags);if(['motorhome','caravan'].includes(vehicle)){if(tags.motorhome==='yes'||tags.caravans==='yes')score+=30;if(tags.power_supply)score+=8;if(tags.sanitary_dump_station==='yes')score+=12}return score}function query(point,trip,radius=12000){const r=trip?.accommodationType||'any',tourism=r==='camping'?'^(camp_site|caravan_site)$':r==='hotel-bnb'?'^(hotel|guest_house|hostel|motel)$':'^(hotel|guest_house|hostel|motel|camp_site|caravan_site)$';return`[out:json][timeout:8][maxsize:4194304];(nwr(around:${radius},${Number(point.lat).toFixed(5)},${Number(point.lon).toFixed(5)})["tourism"~"${tourism}"]["name"];);out center tags 80;`}async function fetchJson(endpoint,q,fetchImpl,timeoutMs){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{const r=await fetchImpl(endpoint,{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded',accept:'application/json'},body:new URLSearchParams({data:q}),signal:controller.signal});if(!r.ok)return null;return await r.json()}catch{return null}finally{clearTimeout(timer)}}function normalize(e){const point={lat:Number(e.lat??e.center?.lat),lon:Number(e.lon??e.center?.lon)},tags=e.tags||{},name=tags.name||tags['name:en']||tags.brand;return valid(point)&&name?{id:`${e.type}-${e.id}`,name,point,tags}:null}export async function enrichOvernightAccommodations(trip,plan,{fetchImpl=globalThis.fetch,timeoutMs=5500}={}){if(typeof fetchImpl!=='function'||trip?.liveData===false)return plan;for(const day of plan?.days||[]){if(day.kind==='return'||day.to===trip.origin||!valid(day.toPoint))continue;let found=[];for(const endpoint of ENDPOINTS){const data=await fetchJson(endpoint,query(day.toPoint,trip),fetchImpl,timeoutMs);found=(data?.elements||[]).map(normalize).filter(Boolean);if(found.length)break}const ranked=found.map(place=>({place,distanceKm:km(day.toPoint,place.point)})).filter(x=>x.distanceKm<=15).map(x=>({...x,score:accommodationSuitability(x.place.tags,trip,x.distanceKm)})).filter(x=>Number.isFinite(x.score)).sort((a,b)=>b.score-a.score),best=ranked[0];if(!best)continue;day.recommendations=(day.recommendations||[]).filter(x=>x.type!=='accommodation');const evidence=[],t=best.place.tags||{};if(trip.transport==='motorcycle'){if(['yes','designated','permissive'].includes(String(t.motorcycle||t['access:motorcycle']||'').toLowerCase()))evidence.push('motor-toegang bevestigd');if(String(t.garage||t['parking:garage']||'').toLowerCase()==='yes')evidence.push('garage');if(String(t.covered||t['parking:covered']||'').toLowerCase()==='yes')evidence.push('overdekte parking');if(t.parking||t['parking:condition'])evidence.push('parking vermeld')}const item={type:'accommodation',name:best.place.name,point:best.place.point,live:true,source:'OpenStreetMap · voertuiggeschikte overnachting',vehicleFit:true,vehicleFitScore:Math.round(best.score),vehicleFitEvidence:evidence,reason:trip.transport==='motorcycle'?`Geselecteerd voor motorreis op verblijfstype, nabijheid en ${evidence.length?evidence.join(', '):'beschikbare parkeer-/voertuiginformatie; controleer details via de kaartlink'}.`:'Geselecteerd op voertuigtype, verblijfstype en nabijheid.',mapUrl:`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(best.place.name+' '+best.place.point.lat+','+best.place.point.lon)}`};day.recommendations.push(item);day.sleepProposal=item}plan.recommendations=(plan.days||[]).flatMap(d=>d.recommendations||[]);return plan}
