ReisSlim v1.7.14 · Build 1714

This package addresses the deployment/update problem as well as retaining the
v1.6.1 loop-route geometry fix.

IMPORTANT
Upload EVERY file from this ZIP to the SAME GitHub Pages repository/folder that
serves ReisSlim. Replace the existing files.

Deployment identity:
- Header/app version: 1.7.14 / 1714
- HTML fingerprint: 1.7.14-1714-loopfix
- Service-worker cache: reisslim-v1.7.14-build-1714
- JS/CSS references are cache-busted with ?v=1714
- New service worker calls skipWaiting() and clients.claim()
- Activation deletes obsolete caches.

After GitHub Pages deploys, opening/reloading ReisSlim should show v1.7.14 · 1714.
If it still shows 1.6.0/1600 or 1.6.1/1601, the browser is not receiving the
new repository files; that is then a deployment/path issue rather than the
application package.

Build 1714 live-discovery repair:
- Private.coffee is now primary; obsolete Kumi endpoint removed.
- Discovery query reduced from 8 seeds x 5 feature families to 4 seed place lookups.
- Two progressive passes maximum; returns immediately after 6 usable live regions.
- Per-endpoint client timeout reduced to 6.5 seconds; Overpass query timeout 6 seconds.
- Response cap reduced to 4 MiB / 120 elements to avoid public-server overload.
