import { validCoordinate } from './config.js';
import { haversineKm } from './route-engine.js';
import { transportId } from './vehicle-intelligence.js';

const NOMINATIM_URL='https://nominatim.openstreetmap.org/search';
const OVERPASS_URL='https://overpass-api.de/api/interpreter';
const WEATHER_URL='https://api.open-meteo.com/v1/forecast';
const CACHE_PREFIX='reisslim.live.v2.';
const clone=value=>typeof globalThis.structuredClone==='function'?globalThis.structuredClone(value):JSON.parse(JSON.stringify(value));
function defaultStorage(){try{return globalThis.localStorage||null}catch{return null}}
function cacheKey(namespace,input){let hash=2166136261;for(const character of String(input)){hash^=character.charCodeAt(0);hash=Math.imul(hash,16777619)}return `${CACHE_PREFIX}${namespace}.${(hash>>>0).toString(36)}`}
function readCache(storage,key,maxAgeMs){if(!storage)return null;try{const record=JSON.parse(storage.getItem(key));return record&&Date.now()-record.savedAt<=maxAgeMs?record.value:null}catch{return null}}
function writeCache(storage,key,value){if(!storage)return;try{storage.setItem(key,JSON.stringify({savedAt:Date.now(),value}))}catch{}}
async function fetchJson(url,options,timeoutMs,fetchImpl){const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),timeoutMs);try{const response=await fetchImpl(url,{...options,signal:controller.signal});if(!response.ok)throw new Error(`Live databron antwoordde met ${response.status}.`);return response.json()}finally{clearTimeout(timeout)}}

export async function geocodeOrigin(origin,options={}){const query=String(origin||'').trim();const fetchImpl=options.fetchImpl||globalThis.fetch;if(!query||typeof fetchImpl!=='function')return null;const storage=options.storage===undefined?defaultStorage():options.storage;const key=cacheKey('geocode',query.toLocaleLowerCase('nl-NL'));const cached=readCache(storage,key,90*24*60*60*1000);if(cached)return cached;const url=new URL(options.nominatimUrl||NOMINATIM_URL);url.search=new URLSearchParams({q:query,format:'jsonv2',limit:'1',addressdetails:'0'});try{const result=await fetchJson(url,{headers:{accept:'application/json'}},7000,fetchImpl);const match=result?.[0];const point=match?{lat:Number(match.lat),lon:Number(match.lon),name:query,source:'OpenStreetMap Nominatim'}:null;if(!validCoordinate(point))return null;writeCache(storage,key,point);return point}catch{return null}}

function uniqueAnchors(plan){const candidates=[];for(const day of plan.days||[]){if(validCoordinate(day.fromPoint))candidates.push(day.fromPoint);if(validCoordinate(day.toPoint)&&!(day.kind==='return'&&day.toPoint.role==='return'))candidates.push(day.toPoint);for(const waypoint of day.waypoints||[])if(validCoordinate(waypoint))candidates.push(waypoint)}const unique=[];for(const point of candidates){if(!unique.some(item=>haversineKm(item,point)<2.5))unique.push({lat:point.lat,lon:point.lon});if(unique.length>=12)break}return unique}
export function buildOverpassQuery(plan){const anchors=uniqueAnchors(plan);const clauses=anchors.flatMap(point=>{const around=`around:12000,${Number(point.lat).toFixed(5)},${Number(point.lon).toFixed(5)}`;return [`nwr(${around})["tourism"~"^(hotel|guest_house|hostel|motel|camp_site|caravan_site)$"]["name"];`,`nwr(${around})["amenity"~"^(restaurant|cafe|fast_food|fuel|charging_station)$"]["name"];`,`nwr(${around})["tourism"~"^(attraction|viewpoint|museum|zoo|theme_park|gallery)$"]["name"];`,`nwr(${around})["historic"]["name"];`,`nwr(${around})["leisure"~"^(nature_reserve|park)$"]["name"];`,`nwr(${around})["highway"~"^(rest_area|services)$"];`]});return `[out:json][timeout:18][maxsize:16777216];(${clauses.join('')});out center tags;`}
function placeType(tags={}){if(['hotel','guest_house','hostel','motel','camp_site','caravan_site'].includes(tags.tourism))return'accommodation';if(['restaurant','cafe','fast_food'].includes(tags.amenity))return'restaurant';if(['fuel','charging_station'].includes(tags.amenity))return'fuel';if(['rest_area','services'].includes(tags.highway))return'rest';if(['attraction','viewpoint','museum','zoo','theme_park','gallery'].includes(tags.tourism)||tags.historic||['nature_reserve','park'].includes(tags.leisure))return'activity';return null}
function mapsSearchUrl(name,point){return `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(`${name} ${Number(point.lat).toFixed(5)},${Number(point.lon).toFixed(5)}`)}`}
function evidenceScore(tags={}){let score=0;if(tags.website||tags['contact:website'])score+=10;if(tags.opening_hours)score+=6;if(tags.phone||tags['contact:phone'])score+=3;if(tags.wikidata||tags.wikipedia)score+=5;if(tags.cuisine)score+=4;if(tags.reservation||tags['reservation:website'])score+=3;if(tags.outdoor_seating==='yes')score+=2;if(tags.wheelchair==='yes')score+=2;const stars=Number(tags.stars);if(Number.isFinite(stars)&&stars>0)score+=Math.min(10,stars*2);return score}
function normalizePlace(element){const point={lat:Number(element.lat??element.center?.lat),lon:Number(element.lon??element.center?.lon)};const type=placeType(element.tags);if(!type||!validCoordinate(point))return null;const tags=element.tags||{};const fallback={accommodation:'Verblijf',restaurant:'Eetgelegenheid',activity:'Bezienswaardigheid',fuel:'Tankstation',rest:'Rustplaats'}[type];const name=tags.name||tags['name:nl']||tags['name:en']||tags.brand||fallback;const officialStars=Number(tags.stars);return{id:`${element.type}-${element.id}`,osmType:element.type,osmId:element.id,type,name,named:name!==fallback,point,tags,openingHours:tags.opening_hours||null,website:tags.website||tags['contact:website']||null,osmUrl:`https://www.openstreetmap.org/${element.type}/${element.id}`,mapUrl:mapsSearchUrl(name,point),evidenceScore:evidenceScore(tags),officialStars:Number.isFinite(officialStars)&&officialStars>0?officialStars:null}}
export function normalizeOverpassPlaces(payload){return(payload?.elements||[]).map(normalizePlace).filter(Boolean)}
function accommodationFits(place,vehicle){const camping=['camp_site','caravan_site'].includes(place.tags?.tourism);if(vehicle==='caravan'||vehicle==='motorhome')return camping;return!camping}
function suitability(place,recommendation,trip){const vehicle=transportId(trip.transport);let score=0;if(place.type===recommendation.type)score+=30;if(recommendation.type==='service'&&['rest','fuel','accommodation'].includes(place.type))score+=12;if(place.type==='accommodation')score+=accommodationFits(place,vehicle)?18:-25;if(place.named)score+=18;score+=place.evidenceScore||0;if(place.type==='restaurant'&&place.tags?.amenity==='fast_food')score-=7;return score}
function proposalReason(place,baseName){const signals=[];if(place.website)signals.push('eigen website');if(place.openingHours)signals.push('openingstijden');if(place.tags?.cuisine)signals.push(`keuken: ${place.tags.cuisine.replaceAll(';',', ')}`);if(place.officialStars)signals.push(`${place.officialStars}-sterren classificatie`);const evidence=signals.length?` Brondata bevat ${signals.slice(0,3).join(', ')}.`:'';return `Specifiek live voorstel nabij ${baseName||'het planpunt'}.${evidence} Open de kaartlink om de actuele reviewscore en recente beoordelingen te controleren; OpenStreetMap zelf levert geen consumentenratings.`}
function applyLivePlace(item,selected,day){const place=selected.place;Object.assign(item,{name:place.name,point:place.point,confidence:'named-provider-evidence',source:'OpenStreetMap via Overpass',verified:false,live:true,genericFallback:false,detourKm:Number(selected.distanceKm.toFixed(1)),openingHours:place.openingHours,websiteUrl:place.website,sourceUrl:place.osmUrl,mapUrl:place.mapUrl,url:place.mapUrl,officialStars:place.officialStars,rating:null,ratingProvider:null,ratingStatus:'controleer actuele reviews via kaartlink',evidenceScore:place.evidenceScore,reason:proposalReason(place,day.overnight||day.location||day.to),lastChecked:new Date().toISOString()})}
function enrichRecommendations(plan,places,trip){const used=new Set();const maximumKm={accommodation:16,restaurant:12,activity:18,fuel:12,rest:12,service:14};for(const day of plan.days||[]){for(const item of day.recommendations||[]){if(!validCoordinate(item.point))continue;const candidates=places.filter(place=>place.named).map(place=>({place,distanceKm:haversineKm(item.point,place.point)})).filter(candidate=>candidate.distanceKm!==null&&candidate.distanceKm<=(maximumKm[item.type]||12)).filter(candidate=>item.type===candidate.place.type||(item.type==='service'&&['rest','fuel','accommodation'].includes(candidate.place.type))).sort((a,b)=>suitability(b.place,item,trip)-suitability(a.place,item,trip)||a.distanceKm-b.distanceKm||a.place.name.localeCompare(b.place.name,'nl'));const selected=candidates.find(candidate=>!used.has(candidate.place.id))||candidates[0];if(!selected)continue;if(!['fuel','rest'].includes(item.type))used.add(selected.place.id);applyLivePlace(item,selected,day)}day.sleepProposal=day.recommendations?.find(item=>item.type==='accommodation')||null}plan.recommendations=(plan.days||[]).flatMap(day=>day.recommendations||[]);return plan}
async function fetchPlaces(plan,options,fetchImpl,storage){const query=buildOverpassQuery(plan);if(!query.includes('nwr('))return[];const key=cacheKey('places',query);const cached=readCache(storage,key,5*24*60*60*1000);if(cached)return cached;const body=new URLSearchParams({data:query}).toString();const payload=await fetchJson(options.overpassUrl||OVERPASS_URL,{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded',accept:'application/json'},body},options.timeoutMs||15000,fetchImpl);const places=normalizeOverpassPlaces(payload);writeCache(storage,key,places);return places}
function dateDifference(dateString,now=new Date()){const date=new Date(`${dateString}T12:00:00`);const today=new Date(now.getFullYear(),now.getMonth(),now.getDate(),12);return Math.round((date-today)/86400000)}
async function fetchWeather(trip,destination,options,fetchImpl,storage){const leadDays=dateDifference(trip.startDate,options.now||new Date());if(leadDays<-1||leadDays>15||!validCoordinate(destination.bases?.[0]))return null;const point=destination.bases[0];const url=new URL(options.weatherUrl||WEATHER_URL);url.search=new URLSearchParams({latitude:String(point.lat),longitude:String(point.lon),daily:'weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max,wind_speed_10m_max',timezone:'auto',forecast_days:'16'});const key=cacheKey('weather',`${point.lat},${point.lon}`);const cached=readCache(storage,key,2*60*60*1000);if(cached)return cached;const payload=await fetchJson(url,{headers:{accept:'application/json'}},options.timeoutMs||8000,fetchImpl);const daily=payload.daily||{};const days=(daily.time||[]).map((date,index)=>({date,weatherCode:daily.weather_code?.[index],minimumC:daily.temperature_2m_min?.[index],maximumC:daily.temperature_2m_max?.[index],precipitationChance:daily.precipitation_probability_max?.[index],windKmh:daily.wind_speed_10m_max?.[index]})).filter(day=>day.date>=trip.startDate).slice(0,trip.days);const weather=days.length?{source:'Open-Meteo',live:true,days,lastChecked:new Date().toISOString()}:null;if(weather)writeCache(storage,key,weather);return weather}
export async function enrichPlanWithPlaces(trip,destination,plan,options={}){if(trip.liveData===false)return plan;const fetchImpl=options.fetchImpl||globalThis.fetch;if(typeof fetchImpl!=='function')return plan;const storage=options.storage===undefined?defaultStorage():options.storage;const next=clone(plan);const[placesResult,weatherResult]=await Promise.allSettled([fetchPlaces(next,options,fetchImpl,storage),fetchWeather(trip,destination,options,fetchImpl,storage)]);const places=placesResult.status==='fulfilled'?placesResult.value:[];if(places.length)enrichRecommendations(next,places,trip);if(weatherResult.status==='fulfilled'&&weatherResult.value)next.weather=weatherResult.value;next.placeData={live:places.length>0,source:places.length?'OpenStreetMap via Overpass':'ReisSlim offline voertuigregels',namedPlaces:places.length,reviewRatingsLive:false,reviewRatingNote:'Actuele consumentenratings worden via de kaartlink gecontroleerd; ReisSlim claimt zonder ratingsprovider geen reviewscore.',weatherLive:Boolean(next.weather?.live),error:placesResult.status==='rejected'?'Live plaatsen niet beschikbaar; offline voorstellen blijven actief.':null};return next}
