{
  "name": "reisslim",
  "version": "1.14.4",
  "private": true,
  "type": "module",
  "description": "Voertuigbewuste, transparante roadtripplanner voor Nederlandse reizigers.",
  "scripts": {
    "test": "node --test --test-reporter=spec",
    "check:syntax": "node scripts/check-syntax.mjs",
    "check:pwa": "node scripts/check-pwa.mjs",
    "check": "node scripts/check-syntax.mjs && node --test --test-reporter=spec && node scripts/check-pwa.mjs"
  },
  "engines": { "node": ">=20" }
}
