ReisSlim v1.7.3 · Build 1703

- Fixed the header 'Nieuwe reis' action.
- The action is now bound before optional UI handlers and no longer depends on a confirm dialog.
- It clears the draft, resets the trip to defaults, opens Planner and scrolls to the top.
- Header branding is now static instead of a button.
- Any accidental nonfunctional header buttons are suppressed; only 'Nieuwe reis' remains interactive.
