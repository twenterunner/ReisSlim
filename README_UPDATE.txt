ReisSlim v1.6.9 · Build 1609

UI / MAP / RANKING UPGRADE

- Removed dashboard "Populaire richtingen" section.
- Removed the "Beperkte stretch-ideeën toestaan" control.
- Stretch proposals are always enabled and receive an explicit ranking penalty,
  so exact matches remain ahead of stretch ideas.
- Each proposal now shows its Top 3 and Bottom 3 criterion scores from the full
  scoring model; the old generic four-score block is hidden.
- Each travel day receives a distinct map route colour.
- POI types receive distinct colours.
- Added a clear route/POI legend under the map.
- Added a complete GPX waypoint/POI list under the map, grouped visibly by day
  and with external map links when available.
- Removed the low-value generic "Waarom deze mix?" notice when no actual warning
  or shortage exists.
- Premium visual pass across cards, planner controls, navigation, map, score
  panels and proposal hierarchy.

Upload ALL files in this ZIP to the repository root. map-view.js is included and
must replace the existing repository copy.
