ReisSlim v1.6.0 · Build 1600

Loop-route geometry fix:
- return corridor lateral separation increased substantially;
- live return routing gets stronger via-point displacement;
- cache/version bumped so old JavaScript and service-worker assets are not reused.

Upload ALL files in this ZIP to the repository root and replace existing files.
